import React from 'react';

const About = () => {
    return (
        <div className="about">
            <p>This app was created using react and react-router-dom to show the virutal linking within React</p>
        </div>
    );
};

export default About;